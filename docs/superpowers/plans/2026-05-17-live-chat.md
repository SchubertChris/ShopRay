# Live-Chat System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das bestehende Ticket-System zu einem Echtzeit-Chat ausbauen — Ticket erstellen = Konversation starten, Verlauf als Chat-Bubbles, Admin antwortet im Support-Tab.

**Architecture:** Neue `ticket_messages`-Tabelle (FK → tickets) speichert alle Nachrichten. Kunde liest/schreibt direkt via Supabase (RLS gesichert, Realtime für Live-Updates). Admin schreibt via Backend-API (service_role), pollt alle 2s bei offenem Chat-Panel.

**Tech Stack:** Supabase (PostgreSQL + Realtime + RLS), Express + Zod (Admin-Endpoints), React + SCSS (Frontend + Admin UI)

---

## Datei-Übersicht

### Neu erstellen
| Datei | Zweck |
|---|---|
| `database/migration_014_ticket_messages.sql` | Tabelle + RLS + Index |
| `Frontend/src/features/tickets/hooks/useTicketChat.ts` | Supabase Realtime Hook |
| `Frontend/src/pages/user/ticket-detail.tsx` | Chat-Detailseite (Kunde) |
| `Frontend/src/sass/pages/user/_ticket-detail.scss` | Chat-Bubble Styles |
| `Backend/src/routes/admin-ticket-messages.ts` | Admin POST/GET Nachrichten |

### Modifizieren
| Datei | Änderung |
|---|---|
| `database/schema.sql` | ticket_messages integrieren |
| `Frontend/src/features/tickets/types/ticket.types.ts` | `TicketMessage` Type |
| `Frontend/src/features/tickets/api/ticketService.ts` | erste Nachricht bei createTicket |
| `Frontend/src/features/tickets/index.ts` | neue Exports |
| `Frontend/src/config/routes.ts` | `TICKET_DETAIL` Route |
| `Frontend/src/App.tsx` | neue Route registrieren |
| `Frontend/src/pages/user/tickets.tsx` | Klick → navigate zu Detail |
| `Frontend/src/pages/support/chat.tsx` | Redirect zu tickets/new |
| `Frontend/src/sass/pages/user/_index.scss` | `_ticket-detail` forwarden |
| `Admin/src/api/adminApi.ts` | getTicketMessages + sendAdminMessage |
| `Admin/src/pages/support/index.tsx` | Chat-Tab + Split-View + Polling |
| `Backend/src/index.ts` | neuen Router registrieren |

---

## Task 1: Datenbank-Migration

**Files:**
- Create: `database/migration_014_ticket_messages.sql`
- Modify: `database/schema.sql`

- [ ] **Schritt 1: Migration-Datei erstellen**

Datei `database/migration_014_ticket_messages.sql`:

```sql
-- Migration 014: ticket_messages — Chat-Nachrichten pro Ticket
-- Ausführen im Supabase SQL-Editor

CREATE TABLE IF NOT EXISTS public.ticket_messages (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ticket_id  UUID NOT NULL REFERENCES public.tickets(id) ON DELETE CASCADE,
  sender     TEXT NOT NULL CHECK (sender IN ('customer', 'admin')),
  text       TEXT NOT NULL CHECK (char_length(text) BETWEEN 1 AND 5000),
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX IF NOT EXISTS ticket_messages_ticket_id_created_at_idx
  ON public.ticket_messages (ticket_id, created_at);

ALTER TABLE public.ticket_messages ENABLE ROW LEVEL SECURITY;

-- Kunde liest nur Nachrichten eigener Tickets
CREATE POLICY "customer_read_own_messages" ON public.ticket_messages
  FOR SELECT USING (
    ticket_id IN (SELECT id FROM public.tickets WHERE user_id = auth.uid())
  );

-- Kunde schreibt nur als 'customer' in eigene Tickets
CREATE POLICY "customer_insert_own_messages" ON public.ticket_messages
  FOR INSERT WITH CHECK (
    sender = 'customer' AND
    ticket_id IN (SELECT id FROM public.tickets WHERE user_id = auth.uid())
  );

GRANT SELECT, INSERT ON public.ticket_messages TO authenticated;

-- Bestehende Ticket-Nachrichten migrieren (erste Nachricht jedes Tickets)
INSERT INTO public.ticket_messages (ticket_id, sender, text, created_at)
SELECT id, 'customer', message, created_at
FROM public.tickets
WHERE message IS NOT NULL AND message != ''
ON CONFLICT DO NOTHING;
```

- [ ] **Schritt 2: schema.sql aktualisieren**

In `database/schema.sql` nach der `tickets`-Tabellendefinition einfügen (vor den Grants):

```sql
-- ── ticket_messages ──────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS public.ticket_messages (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ticket_id  UUID NOT NULL REFERENCES public.tickets(id) ON DELETE CASCADE,
  sender     TEXT NOT NULL CHECK (sender IN ('customer', 'admin')),
  text       TEXT NOT NULL CHECK (char_length(text) BETWEEN 1 AND 5000),
  created_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX IF NOT EXISTS ticket_messages_ticket_id_created_at_idx
  ON public.ticket_messages (ticket_id, created_at);
ALTER TABLE public.ticket_messages ENABLE ROW LEVEL SECURITY;
CREATE POLICY "customer_read_own_messages" ON public.ticket_messages
  FOR SELECT USING (ticket_id IN (SELECT id FROM public.tickets WHERE user_id = auth.uid()));
CREATE POLICY "customer_insert_own_messages" ON public.ticket_messages
  FOR INSERT WITH CHECK (
    sender = 'customer' AND
    ticket_id IN (SELECT id FROM public.tickets WHERE user_id = auth.uid())
  );
GRANT SELECT, INSERT ON public.ticket_messages TO authenticated;
```

- [ ] **Schritt 3: Migration im Supabase SQL-Editor ausführen**

URL: https://supabase.com/dashboard/project/ikopnwugsohiehzpxxzu/sql/new  
Inhalt von `migration_014_ticket_messages.sql` einfügen → Run  
Erwartung: „Success. No rows returned"

- [ ] **Schritt 4: Commit**

```bash
git add database/migration_014_ticket_messages.sql database/schema.sql
git commit -m "feat: migration_014 — ticket_messages Tabelle mit RLS"
```

---

## Task 2: Frontend — Types + Service erweitern

**Files:**
- Modify: `Frontend/src/features/tickets/types/ticket.types.ts`
- Modify: `Frontend/src/features/tickets/api/ticketService.ts`
- Modify: `Frontend/src/features/tickets/index.ts`

- [ ] **Schritt 1: TicketMessage Type hinzufügen**

In `Frontend/src/features/tickets/types/ticket.types.ts` am Ende anfügen:

```typescript
export interface TicketMessage {
  id:        string;
  ticketId:  string;
  sender:    'customer' | 'admin';
  text:      string;
  createdAt: string;
}
```

- [ ] **Schritt 2: Service-Funktionen hinzufügen**

In `Frontend/src/features/tickets/api/ticketService.ts` nach den Imports einfügen:

```typescript
import type { TicketMessage } from '../types/ticket.types';

function mapMessage(raw: Record<string, unknown>): TicketMessage {
  return {
    id:        String(raw.id ?? ''),
    ticketId:  String(raw.ticket_id ?? ''),
    sender:    (raw.sender as 'customer' | 'admin') ?? 'customer',
    text:      String(raw.text ?? ''),
    createdAt: String(raw.created_at ?? ''),
  };
}

export async function getTicketMessages(ticketId: string): Promise<TicketMessage[]> {
  const { data, error } = await supabase
    .from('ticket_messages')
    .select('*')
    .eq('ticket_id', ticketId)
    .order('created_at', { ascending: true });
  if (error) throw error;
  return (data as Record<string, unknown>[]).map(mapMessage);
}

export async function sendTicketMessage(ticketId: string, text: string): Promise<TicketMessage> {
  const { data, error } = await supabase
    .from('ticket_messages')
    .insert({ ticket_id: ticketId, sender: 'customer', text: text.trim() })
    .select('*')
    .single();
  if (error) throw error;
  return mapMessage(data as Record<string, unknown>);
}
```

- [ ] **Schritt 3: createTicket erweitern — erste Nachricht anlegen**

In `ticketService.ts` die Funktion `createTicket` wie folgt ersetzen:

```typescript
export async function createTicket(payload: TicketPayload): Promise<Ticket> {
  const { data: { user }, error: authErr } = await supabase.auth.getUser();
  if (authErr || !user) throw authErr ?? new Error('Nicht eingeloggt');

  const { data, error } = await supabase
    .from('tickets')
    .insert({
      user_id:  user.id,
      subject:  `[${payload.priority}] ${payload.subject}`,
      message:  payload.description,
      category: CATEGORY_TO_DB[payload.category] ?? 'other',
    })
    .select('*')
    .single();
  if (error) throw error;

  // Erste Nachricht als Chat-Eintrag anlegen
  await supabase
    .from('ticket_messages')
    .insert({ ticket_id: data.id, sender: 'customer', text: payload.description });

  return mapTicket(data as Record<string, unknown>);
}
```

- [ ] **Schritt 4: Exports in index.ts (ohne Hook — kommt nach Task 3)**

`Frontend/src/features/tickets/index.ts` ersetzen mit:

```typescript
// ── OPTIONALES FEATURE ────────────────────────────────────────────────────────
export { getTickets, getTicketById, createTicket, getTicketMessages, sendTicketMessage } from './api/ticketService';
export type { Ticket, TicketPayload, TicketStatus, TicketPriority, TicketCategory, TicketMessage } from './types/ticket.types';
```

- [ ] **Schritt 5: TypeScript-Check**

```bash
cd Frontend && npx tsc --noEmit
```
Erwartung: keine Fehler

---

## Task 3: Frontend — useTicketChat Hook (Supabase Realtime)

**Files:**
- Create: `Frontend/src/features/tickets/hooks/useTicketChat.ts`

- [ ] **Schritt 1: Hook erstellen**

Neue Datei `Frontend/src/features/tickets/hooks/useTicketChat.ts`:

```typescript
import { useState, useEffect, useRef, useCallback } from 'react';
import { supabase } from '@/lib/supabase';
import { getTicketMessages, sendTicketMessage } from '../api/ticketService';
import type { TicketMessage } from '../types/ticket.types';

export function useTicketChat(ticketId: string) {
  const [messages, setMessages] = useState<TicketMessage[]>([]);
  const [loading,  setLoading]  = useState(true);
  const [sending,  setSending]  = useState(false);
  const [error,    setError]    = useState<string | null>(null);
  const channelRef = useRef<ReturnType<typeof supabase.channel> | null>(null);

  useEffect(() => {
    if (!ticketId) return;

    setLoading(true);
    getTicketMessages(ticketId)
      .then(setMessages)
      .catch(() => setError('Nachrichten konnten nicht geladen werden.'))
      .finally(() => setLoading(false));

    // Supabase Realtime — neue Nachrichten sofort anzeigen
    const channel = supabase
      .channel(`ticket_messages:${ticketId}`)
      .on(
        'postgres_changes',
        { event: 'INSERT', schema: 'public', table: 'ticket_messages', filter: `ticket_id=eq.${ticketId}` },
        (payload) => {
          const raw = payload.new as Record<string, unknown>;
          const msg: TicketMessage = {
            id:        String(raw.id ?? ''),
            ticketId:  String(raw.ticket_id ?? ''),
            sender:    (raw.sender as 'customer' | 'admin') ?? 'customer',
            text:      String(raw.text ?? ''),
            createdAt: String(raw.created_at ?? ''),
          };
          setMessages(prev => [...prev, msg]);
        }
      )
      .subscribe();

    channelRef.current = channel;

    return () => {
      supabase.removeChannel(channel);
    };
  }, [ticketId]);

  const send = useCallback(async (text: string): Promise<void> => {
    const trimmed = text.trim();
    if (!trimmed || sending) return;
    setSending(true);
    try {
      await sendTicketMessage(ticketId, trimmed);
      // Realtime fügt die Nachricht automatisch zum State hinzu
    } catch {
      setError('Nachricht konnte nicht gesendet werden.');
    } finally {
      setSending(false);
    }
  }, [ticketId, sending]);

  return { messages, loading, sending, error, send };
}
```

- [ ] **Schritt 2: index.ts — Hook-Export ergänzen**

`Frontend/src/features/tickets/index.ts` ersetzen mit (jetzt vollständig):

```typescript
// ── OPTIONALES FEATURE ────────────────────────────────────────────────────────
export { getTickets, getTicketById, createTicket, getTicketMessages, sendTicketMessage } from './api/ticketService';
export { useTicketChat } from './hooks/useTicketChat';
export type { Ticket, TicketPayload, TicketStatus, TicketPriority, TicketCategory, TicketMessage } from './types/ticket.types';
```

- [ ] **Schritt 3: TypeScript-Check**

```bash
cd Frontend && npx tsc --noEmit
```
Erwartung: keine Fehler

- [ ] **Schritt 4: Commit**

```bash
git add Frontend/src/features/tickets/
git commit -m "feat: TicketMessage type + service + useTicketChat Realtime Hook"
```

---

## Task 4: Frontend — Routes + App.tsx

**Files:**
- Modify: `Frontend/src/config/routes.ts`
- Modify: `Frontend/src/App.tsx`

- [ ] **Schritt 1: Route in routes.ts eintragen**

In `Frontend/src/config/routes.ts` im `ACCOUNT`-Block ergänzen:

```typescript
ACCOUNT: {
  ROOT:          '/account',
  DASHBOARD:     '/account/dashboard',
  ORDERS:        '/account/orders',
  ORDER_DETAIL:  '/account/orders/:id',
  orderDetail:   (id: string) => `/account/orders/${id}`,
  WISHLIST:      '/account/wishlist',
  TICKETS:       '/account/tickets',
  TICKET_NEW:    '/account/tickets/new',
  TICKET_DETAIL: '/account/tickets/:id',
  ticketDetail:  (id: string) => `/account/tickets/${id}`,
  SETTINGS:      '/account/settings',
  ADDRESSES:     '/account/addresses',
  MY_DATA:       '/account/my-data',
},
```

- [ ] **Schritt 2: Route in App.tsx registrieren**

In `Frontend/src/App.tsx` die Ticket-Routen suchen und `ticket-detail` hinzufügen. Die genaue Stelle hängt von der bestehenden Routing-Struktur ab. Suche nach dem Import von `ticket-new` und ergänze darunter:

```typescript
import TicketDetailPage from '@pages/user/ticket-detail';
```

Und in der Route-Definition (nach der ticket-new Route):

```tsx
<Route path={ROUTES.ACCOUNT.TICKET_DETAIL} element={<TicketDetailPage />} />
```

- [ ] **Schritt 3: TypeScript-Check**

```bash
cd Frontend && npx tsc --noEmit
```
Erwartung: Fehler wegen fehlendem `ticket-detail.tsx` — das ist OK, kommt in Task 5.

---

## Task 5: Frontend — Ticket-Detailseite (Chat UI)

**Files:**
- Create: `Frontend/src/pages/user/ticket-detail.tsx`

- [ ] **Schritt 1: Seite erstellen**

Neue Datei `Frontend/src/pages/user/ticket-detail.tsx`:

```typescript
import { useRef, useEffect, useState, FormEvent } from 'react';
import { useParams, Link, Navigate } from 'react-router-dom';
import { SeoMeta } from '@components/ui';
import { ROUTES } from '@config/routes';
import { getTicketById } from '@features/tickets';
import { useTicketChat } from '@features/tickets';
import type { Ticket } from '@features/tickets';

const STATUS_LABEL: Record<string, string> = {
  'open':        'Offen',
  'in-progress': 'In Bearbeitung',
  'resolved':    'Gelöst',
  'closed':      'Geschlossen',
};

function formatTime(iso: string): string {
  return new Date(iso).toLocaleString('de-DE', {
    day: '2-digit', month: '2-digit', year: 'numeric',
    hour: '2-digit', minute: '2-digit',
  });
}

export default function TicketDetailPage() {
  const { id } = useParams<{ id: string }>();
  const [ticket,  setTicket]  = useState<Ticket | null>(null);
  const [tLoading, setTLoading] = useState(true);
  const [input,   setInput]   = useState('');
  const bottomRef = useRef<HTMLDivElement>(null);

  const { messages, loading, sending, error, send } = useTicketChat(id ?? '');

  useEffect(() => {
    if (!id) return;
    getTicketById(id)
      .then(setTicket)
      .catch(() => null)
      .finally(() => setTLoading(false));
  }, [id]);

  // Automatisch nach unten scrollen bei neuer Nachricht
  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  if (!id) return <Navigate to={ROUTES.ACCOUNT.TICKETS} replace />;

  const isClosed = ticket?.status === 'closed' || ticket?.status === 'resolved';

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    if (!input.trim() || isClosed) return;
    await send(input);
    setInput('');
  };

  return (
    <>
      <SeoMeta title={ticket ? ticket.subject : 'Ticket'} noIndex />
      <div className="ticket-detail">

        <Link to={ROUTES.ACCOUNT.TICKETS} className="ticket-detail__back">
          ← Zurück zu Tickets
        </Link>

        {!tLoading && ticket && (
          <div className="ticket-detail__header">
            <h1 className="ticket-detail__title">{ticket.subject}</h1>
            <span className={`ticket-status ticket-status--${ticket.status}`}>
              {STATUS_LABEL[ticket.status] ?? ticket.status}
            </span>
          </div>
        )}

        <div className="ticket-chat">
          <div className="ticket-chat__messages">
            {loading && <p className="ticket-chat__loading">Lädt…</p>}

            {!loading && messages.map(msg => (
              <div
                key={msg.id}
                className={`chat-bubble chat-bubble--${msg.sender}`}
              >
                <div className="chat-bubble__text">{msg.text}</div>
                <div className="chat-bubble__time">{formatTime(msg.createdAt)}</div>
              </div>
            ))}

            {!loading && messages.length === 0 && (
              <p className="ticket-chat__empty">Noch keine Nachrichten.</p>
            )}

            <div ref={bottomRef} />
          </div>

          <form className="ticket-chat__input-bar" onSubmit={handleSubmit}>
            {isClosed ? (
              <p className="ticket-chat__closed-note">
                Gespräch geschlossen —{' '}
                <Link to={ROUTES.ACCOUNT.TICKET_NEW}>neues Ticket erstellen</Link>
              </p>
            ) : (
              <>
                <input
                  className="ticket-chat__input"
                  type="text"
                  placeholder="Nachricht schreiben…"
                  value={input}
                  onChange={e => setInput(e.target.value)}
                  disabled={sending}
                  maxLength={5000}
                  aria-label="Nachricht"
                />
                <button
                  className="ticket-chat__send"
                  type="submit"
                  disabled={sending || !input.trim()}
                  aria-label="Senden"
                >
                  {sending ? '…' : '→'}
                </button>
              </>
            )}
          </form>

          {error && <p className="ticket-chat__error">{error}</p>}
        </div>
      </div>
    </>
  );
}
```

- [ ] **Schritt 2: TypeScript-Check**

```bash
cd Frontend && npx tsc --noEmit
```
Erwartung: keine Fehler

---

## Task 6: Frontend — SCSS für Chat-UI

**Files:**
- Create: `Frontend/src/sass/pages/user/_ticket-detail.scss`
- Modify: `Frontend/src/sass/pages/user/_index.scss`

- [ ] **Schritt 1: SCSS-Datei erstellen**

Neue Datei `Frontend/src/sass/pages/user/_ticket-detail.scss`:

```scss
@use '../../abstracts/mixins' as *;
@use '../../abstracts/functions' as *;

// ─── TICKET-DETAIL (CHAT) ──────────────────────────────────────────────────
.ticket-detail {
  padding-block: 0.5rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;

  &__back {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: rem(13px);
    color: var(--clr-text);
    opacity: 0.45;
    @include transition(opacity);
    &:hover { opacity: 0.75; }
  }

  &__header {
    @include flex-between;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  &__title {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    font-weight: 800;
    color: var(--clr-text);
    margin: 0;
  }
}

// ─── CHAT-CONTAINER ────────────────────────────────────────────────────────
.ticket-chat {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--clr-secondary);
  border-radius: 16px;
  overflow: hidden;
  background: var(--clr-surface);

  &__messages {
    flex: 1;
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    min-height: 340px;
    max-height: 520px;
    overflow-y: auto;
    scroll-behavior: smooth;
  }

  &__loading,
  &__empty {
    font-size: rem(13px);
    color: color-mix(in srgb, var(--clr-text) 40%, transparent);
    text-align: center;
    margin: auto;
  }

  &__input-bar {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    padding: 0.875rem 1.25rem;
    border-top: 1px solid var(--clr-secondary);
    background: color-mix(in srgb, var(--clr-surface) 80%, var(--clr-background));
  }

  &__input {
    flex: 1;
    background: var(--clr-background);
    border: 1px solid var(--clr-secondary);
    border-radius: 10px;
    padding: 0.625rem 0.875rem;
    font-size: rem(14px);
    color: var(--clr-text);
    outline: none;
    @include transition(border-color);

    &:focus { border-color: var(--clr-primary); }
    &::placeholder { color: color-mix(in srgb, var(--clr-text) 35%, transparent); }
  }

  &__send {
    width: 38px;
    height: 38px;
    border-radius: 10px;
    border: none;
    background: var(--clr-primary);
    color: #fff;
    font-size: rem(16px);
    cursor: pointer;
    @include flex-center;
    flex-shrink: 0;
    @include transition(opacity);

    &:disabled { opacity: 0.4; cursor: default; }
    &:not(:disabled):hover { opacity: 0.85; }
  }

  &__closed-note {
    font-size: rem(13px);
    color: color-mix(in srgb, var(--clr-text) 55%, transparent);
    text-align: center;
    width: 100%;

    a { color: var(--clr-primary); text-decoration: none; }
  }

  &__error {
    font-size: rem(12px);
    color: #ef4444;
    padding: 0.5rem 1.25rem;
    margin: 0;
  }
}

// ─── CHAT-BUBBLES ──────────────────────────────────────────────────────────
.chat-bubble {
  display: flex;
  flex-direction: column;
  max-width: 75%;

  &--customer {
    align-self: flex-end;
    align-items: flex-end;

    .chat-bubble__text {
      background: var(--clr-primary);
      color: #fff;
      border-radius: 14px 14px 4px 14px;
    }
  }

  &--admin {
    align-self: flex-start;
    align-items: flex-start;

    .chat-bubble__text {
      background: color-mix(in srgb, var(--clr-text) 8%, transparent);
      color: var(--clr-text);
      border-radius: 14px 14px 14px 4px;
    }
  }

  &__text {
    padding: 0.625rem 0.875rem;
    font-size: rem(14px);
    line-height: 1.55;
    word-break: break-word;
  }

  &__time {
    font-size: rem(10.5px);
    color: color-mix(in srgb, var(--clr-text) 35%, transparent);
    margin-top: 0.25rem;
    padding-inline: 0.25rem;
  }
}
```

- [ ] **Schritt 2: In `_index.scss` forwarden**

In `Frontend/src/sass/pages/user/_index.scss` die Zeile ergänzen:

```scss
@forward 'ticket-detail';
```

- [ ] **Schritt 3: Commit**

```bash
git add Frontend/src/pages/user/ticket-detail.tsx \
        Frontend/src/config/routes.ts \
        Frontend/src/App.tsx \
        Frontend/src/sass/pages/user/
git commit -m "feat: Ticket-Detailseite als Chat + SCSS Bubbles"
```

---

## Task 7: Frontend — Tickets-Liste + /chat Redirect

**Files:**
- Modify: `Frontend/src/pages/user/tickets.tsx`
- Modify: `Frontend/src/pages/support/chat.tsx`

- [ ] **Schritt 1: tickets.tsx — Zeile klickbar machen**

In `Frontend/src/pages/user/tickets.tsx`:

Import ergänzen:
```typescript
import { useNavigate } from 'react-router-dom';
```

Im Komponenten-Body:
```typescript
const navigate = useNavigate();
```

Das bestehende `<div key={ticket.id} className="ticket-card">` ersetzen mit:

```tsx
<div
  key={ticket.id}
  className="ticket-card ticket-card--clickable"
  onClick={() => navigate(ROUTES.ACCOUNT.ticketDetail(ticket.id))}
  role="button"
  tabIndex={0}
  onKeyDown={e => e.key === 'Enter' && navigate(ROUTES.ACCOUNT.ticketDetail(ticket.id))}
>
  <div className="ticket-card__body">
    <div className="ticket-card__subject">{ticket.subject}</div>
    <div className="ticket-card__meta">
      {ticket.category} · {new Date(ticket.createdAt).toLocaleDateString('de-DE')}
    </div>
  </div>
  <span className={`ticket-status ticket-status--${ticket.status}`}>
    {STATUS_LABEL[ticket.status]}
  </span>
</div>
```

In `Frontend/src/sass/pages/user/_tickets.scss` ergänzen:

```scss
.ticket-card--clickable {
  cursor: pointer;
  @include transition(background, border-color);
  &:hover { background: color-mix(in srgb, var(--clr-primary) 4%, var(--clr-surface)); }
}
```

- [ ] **Schritt 2: /chat — Redirect einbauen**

`Frontend/src/pages/support/chat.tsx` — ganz oben nach den Imports ergänzen:

```typescript
import { Navigate } from 'react-router-dom';
import { ROUTES }   from '@config/routes';
```

Die gesamte return-Anweisung der Funktion `ChatPage` ersetzen mit:

```tsx
return <Navigate to={ROUTES.ACCOUNT.TICKET_NEW} replace />;
```

Der Rest der Datei (PROVIDERS, MOCK_MESSAGES etc.) kann bleiben — er wird nicht mehr gerendert, schadet aber nicht. Oder vollständig aufräumen und nur den Redirect behalten:

```typescript
import { Navigate } from 'react-router-dom';
import { ROUTES }   from '@config/routes';

export default function ChatPage() {
  return <Navigate to={ROUTES.ACCOUNT.TICKET_NEW} replace />;
}
```

- [ ] **Schritt 3: TypeScript-Check**

```bash
cd Frontend && npx tsc --noEmit
```
Erwartung: keine Fehler

- [ ] **Schritt 4: Commit**

```bash
git add Frontend/src/pages/user/tickets.tsx \
        Frontend/src/pages/support/chat.tsx \
        Frontend/src/sass/pages/user/_tickets.scss
git commit -m "feat: Ticket-Liste klickbar + /chat Redirect zu tickets/new"
```

---

## Task 8: Backend — Admin Nachrichten-Endpoints

**Files:**
- Create: `Backend/src/routes/admin-ticket-messages.ts`
- Modify: `Backend/src/index.ts`

- [ ] **Schritt 1: Route-Datei erstellen**

Neue Datei `Backend/src/routes/admin-ticket-messages.ts`:

```typescript
import { z } from 'zod';
import { Router, Request, Response, NextFunction } from 'express';
import { requireAdmin } from '../middleware/adminAuth';
import { supabase }     from '../lib/supabase';
import { validate }     from '../lib/validate';
import webpush          from 'web-push';

const router = Router();

const MessageSchema = z.object({
  text: z.string().min(1).max(5000),
});

// GET /api/admin/tickets/:id/messages
router.get('/:id/messages', requireAdmin, async (req: Request, res: Response, next: NextFunction): Promise<void> => {
  try {
    const { data, error } = await supabase
      .from('ticket_messages')
      .select('*')
      .eq('ticket_id', req.params.id)
      .order('created_at', { ascending: true });

    if (error) throw error;
    res.json(data ?? []);
  } catch (err) {
    next(err);
  }
});

// POST /api/admin/tickets/:id/messages — Admin antwortet
router.post('/:id/messages', requireAdmin, validate(MessageSchema), async (req: Request, res: Response, next: NextFunction): Promise<void> => {
  try {
    const { text } = req.body as z.infer<typeof MessageSchema>;
    const ticketId = req.params.id;

    // Nachricht in ticket_messages
    const { data: msg, error: msgErr } = await supabase
      .from('ticket_messages')
      .insert({ ticket_id: ticketId, sender: 'admin', text })
      .select('*')
      .single();
    if (msgErr) throw msgErr;

    // tickets.reply + replied_at für E-Mail-Kompatibilität aktualisieren
    await supabase
      .from('tickets')
      .update({ reply: text, replied_at: new Date().toISOString(), updated_at: new Date().toISOString() })
      .eq('id', ticketId);

    // Push-Notification an Kunden (nicht-blockierend)
    try {
      const { data: ticket } = await supabase
        .from('tickets')
        .select('user_id')
        .eq('id', ticketId)
        .single();

      if (ticket?.user_id) {
        const { data: subs } = await supabase
          .from('push_subscriptions')
          .select('endpoint, p256dh, auth')
          .eq('user_id', ticket.user_id);

        if (subs && subs.length > 0) {
          webpush.setVapidDetails(
            `mailto:${process.env.VAPID_EMAIL ?? 'admin@shopray.de'}`,
            process.env.VAPID_PUBLIC_KEY  ?? '',
            process.env.VAPID_PRIVATE_KEY ?? '',
          );
          const payload = JSON.stringify({ title: 'Neue Antwort', body: 'Du hast eine Antwort auf dein Support-Ticket erhalten.' });
          await Promise.allSettled(
            subs.map(s => webpush.sendNotification({ endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } }, payload))
          );
        }
      }
    } catch { /* Push-Fehler nicht an Client weitergeben */ }

    res.status(201).json(msg);
  } catch (err) {
    next(err);
  }
});

export default router;
```

- [ ] **Schritt 2: Router in index.ts registrieren**

In `Backend/src/index.ts` Import ergänzen:

```typescript
import adminTicketMessagesRouter from './routes/admin-ticket-messages';
```

Und nach der `adminTicketsRouter`-Zeile:

```typescript
app.use('/api/admin/tickets', adminTicketMessagesRouter);
```

- [ ] **Schritt 3: TypeScript-Check**

```bash
cd Backend && npx tsc --noEmit
```
Erwartung: keine Fehler

- [ ] **Schritt 4: Commit**

```bash
git add Backend/src/routes/admin-ticket-messages.ts Backend/src/index.ts
git commit -m "feat: Admin GET+POST /api/admin/tickets/:id/messages mit Push"
```

---

## Task 9: Admin — API-Funktionen + Chat-Tab

**Files:**
- Modify: `Admin/src/api/adminApi.ts`
- Modify: `Admin/src/pages/support/index.tsx`

- [ ] **Schritt 1: adminApi.ts erweitern**

In `Admin/src/api/adminApi.ts` folgende Typen und Funktionen ergänzen:

```typescript
export interface TicketMessage {
  id:         string;
  ticket_id:  string;
  sender:     'customer' | 'admin';
  text:       string;
  created_at: string;
}

export async function getAdminTicketMessages(ticketId: string): Promise<TicketMessage[]> {
  const res = await apiFetch(`/api/admin/tickets/${ticketId}/messages`);
  if (!res.ok) throw new Error('Nachrichten konnten nicht geladen werden');
  return res.json();
}

export async function sendAdminMessage(ticketId: string, text: string): Promise<TicketMessage> {
  const res = await apiFetch(`/api/admin/tickets/${ticketId}/messages`, {
    method: 'POST',
    body:   JSON.stringify({ text }),
  });
  if (!res.ok) throw new Error('Senden fehlgeschlagen');
  return res.json();
}
```

- [ ] **Schritt 2: Chat-Tab in support/index.tsx einbauen**

In `Admin/src/pages/support/index.tsx` folgende Ergänzungen vornehmen:

**Imports ergänzen** (bestehende useState/useEffect Import-Zeile um `useRef` erweitern):
```typescript
import { useState, useEffect, useRef } from 'react';
import { getAdminTicketMessages, sendAdminMessage, type TicketMessage } from '../../api/adminApi';
```

**State ergänzen** (nach den bestehenden States):
```typescript
const [chatView,    setChatView]    = useState(false);
const [chatTicket,  setChatTicket]  = useState<AdminTicket | null>(null);
const [chatMsgs,    setChatMsgs]    = useState<TicketMessage[]>([]);
const [chatInput,   setChatInput]   = useState('');
const [chatLoading, setChatLoading] = useState(false);
const [chatSending, setChatSending] = useState(false);
const pollRef = useRef<ReturnType<typeof setInterval> | null>(null);
```

**Chat öffnen:**
```typescript
const openChat = async (ticket: AdminTicket) => {
  setChatTicket(ticket);
  setChatView(true);
  setChatLoading(true);
  try {
    const msgs = await getAdminTicketMessages(ticket.id);
    setChatMsgs(msgs);
  } finally {
    setChatLoading(false);
  }
  // Polling alle 2s
  if (pollRef.current) clearInterval(pollRef.current);
  pollRef.current = setInterval(async () => {
    const fresh = await getAdminTicketMessages(ticket.id).catch(() => null);
    if (fresh) setChatMsgs(fresh);
  }, 2000);
};

const closeChat = () => {
  if (pollRef.current) clearInterval(pollRef.current);
  pollRef.current = null;
  setChatView(false);
  setChatTicket(null);
  setChatMsgs([]);
};

const sendChat = async () => {
  if (!chatTicket || !chatInput.trim() || chatSending) return;
  setChatSending(true);
  try {
    const msg = await sendAdminMessage(chatTicket.id, chatInput.trim());
    setChatMsgs(prev => [...prev, msg]);
    setChatInput('');
  } finally {
    setChatSending(false);
  }
};

// Cleanup beim Unmount
useEffect(() => () => { if (pollRef.current) clearInterval(pollRef.current); }, []);
```

**Chat-Tab in der Tab-Leiste:**

Neben den bestehenden Tabs einen neuen Button für „Chats" ergänzen. Die genaue Position hängt von der bestehenden Tab-Struktur ab — als eigenständiger Toggle neben `viewMode`:

```tsx
<button
  className={`admin-tab${chatView ? ' is-active' : ''}`}
  onClick={() => chatView ? closeChat() : setChatView(true)}
>
  Chats
</button>
```

**Chat-Liste + Chat-Panel** wenn `chatView` aktiv:

```tsx
{chatView && !chatTicket && (
  <div className="tickets-list">
    {tickets
      .filter(t => t.status !== 'closed')
      .map(ticket => (
        <div
          key={ticket.id}
          className="ticket-card ticket-card--clickable"
          onClick={() => openChat(ticket)}
          role="button"
          tabIndex={0}
          onKeyDown={e => e.key === 'Enter' && openChat(ticket)}
        >
          <div className="ticket-card__body">
            <div className="ticket-card__subject">{ticket.subject}</div>
            <div className="ticket-card__meta">
              {ticket.profiles?.name ?? '—'} · {new Date(ticket.created_at).toLocaleDateString('de-DE')}
            </div>
          </div>
          <span className={`admin-badge admin-badge--${ticket.status}`}>
            {STATUS_LABELS[ticket.status]}
          </span>
        </div>
      ))}
  </div>
)}

{chatView && chatTicket && (
  <div className="admin-chat-panel">
    <div className="admin-chat-panel__header">
      <button className="btn-ghost btn-sm" onClick={closeChat}>← Zurück</button>
      <span className="admin-chat-panel__subject">{chatTicket.subject}</span>
    </div>
    <div className="admin-chat-panel__messages">
      {chatLoading && <p>Lädt…</p>}
      {!chatLoading && chatMsgs.map(msg => (
        <div key={msg.id} className={`chat-bubble chat-bubble--${msg.sender === 'admin' ? 'admin-sent' : 'customer-recv'}`}>
          <div className="chat-bubble__text">{msg.text}</div>
          <div className="chat-bubble__time">
            {new Date(msg.created_at).toLocaleString('de-DE', { day:'2-digit', month:'2-digit', hour:'2-digit', minute:'2-digit' })}
          </div>
        </div>
      ))}
    </div>
    <div className="admin-chat-panel__input-bar">
      <input
        className="form-input"
        type="text"
        placeholder="Antwort schreiben…"
        value={chatInput}
        onChange={e => setChatInput(e.target.value)}
        onKeyDown={e => e.key === 'Enter' && sendChat()}
        disabled={chatSending}
        maxLength={5000}
      />
      <button className="btn-primary" onClick={sendChat} disabled={chatSending || !chatInput.trim()}>
        {chatSending ? '…' : 'Senden'}
      </button>
    </div>
  </div>
)}
```

- [ ] **Schritt 3: Admin SCSS für Chat-Panel**

In `Admin/src/sass/pages/support/_support.scss` (oder passende bestehende Datei) ergänzen:

```scss
.admin-chat-panel {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--clr-secondary);
  border-radius: 14px;
  overflow: hidden;
  background: var(--clr-surface);

  &__header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.875rem 1.25rem;
    border-bottom: 1px solid var(--clr-secondary);
  }

  &__subject {
    font-size: rem(14px);
    font-weight: 600;
    color: var(--clr-text);
  }

  &__messages {
    flex: 1;
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    min-height: 300px;
    max-height: 460px;
    overflow-y: auto;
  }

  &__input-bar {
    display: flex;
    gap: 0.625rem;
    padding: 0.875rem 1.25rem;
    border-top: 1px solid var(--clr-secondary);
  }
}

.chat-bubble--admin-sent {
  align-self: flex-end;
  align-items: flex-end;
  max-width: 75%;

  .chat-bubble__text {
    background: var(--clr-primary);
    color: #fff;
    border-radius: 14px 14px 4px 14px;
    padding: 0.6rem 0.875rem;
    font-size: rem(13px);
    line-height: 1.5;
  }
}

.chat-bubble--customer-recv {
  align-self: flex-start;
  max-width: 75%;

  .chat-bubble__text {
    background: color-mix(in srgb, var(--clr-text) 8%, transparent);
    color: var(--clr-text);
    border-radius: 14px 14px 14px 4px;
    padding: 0.6rem 0.875rem;
    font-size: rem(13px);
    line-height: 1.5;
  }
}

.chat-bubble__time {
  font-size: rem(10px);
  color: color-mix(in srgb, var(--clr-text) 35%, transparent);
  margin-top: 0.2rem;
  padding-inline: 0.25rem;
}
```

- [ ] **Schritt 4: TypeScript-Check**

```bash
cd Admin && npx tsc --noEmit
```
Erwartung: keine Fehler

- [ ] **Schritt 5: Commit**

```bash
git add Admin/src/api/adminApi.ts Admin/src/pages/support/index.tsx Admin/src/sass/
git commit -m "feat: Admin Chat-Tab mit Polling + Nachrichten senden"
```

---

## Task 10: Finaler Check + Push

- [ ] **Schritt 1: TypeScript alle 3 Projekte**

```bash
cd Frontend && npx tsc --noEmit && echo "Frontend OK"
cd ../Admin  && npx tsc --noEmit && echo "Admin OK"
cd ../Backend && npx tsc --noEmit && echo "Backend OK"
```
Erwartung: alle 3 `OK`

- [ ] **Schritt 2: Manueller Smoke-Test**

1. Frontend: Ticket erstellen → unter `/account/tickets` Zeile klicken → Chat-Detailseite öffnet mit erster Nachricht
2. Frontend: Nachricht tippen + senden → erscheint als Bubble
3. Admin: `/admin/support` → „Chats"-Tab → Ticket anklicken → Nachrichtenverlauf sichtbar
4. Admin: Antwort senden → im Frontend (offener Tab) erscheint Antwort via Realtime
5. `/chat` aufrufen → landet auf `/account/tickets/new`

- [ ] **Schritt 3: Final-Commit + Push**

```bash
git add -A
git commit -m "feat: Live-Chat System — ticket_messages, Realtime Frontend, Admin Polling"
git push origin main
```

Erwartung: alle 3 Vercel-Projekte bauen sauber (Status READY)
